<% @reviews.each do |review| %>
<div class="card">
  <div class="card-body">
    <div class="row">
      <%= review.user.name %>:&nbsp;<%= review.body.html_safe %>
    </div>
  </div>
</div>
<br/>
<% end %>
<div>
  <ul class="pagination">
    <%= paginate @reviews %>
  </ul>
</div>
